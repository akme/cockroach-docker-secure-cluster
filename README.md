# CockroachDB Docker Secure Cluster
## Start Initial node
```
$ ./run.sh
```
## Create new node and join an existing cluster
```
$ ./run.sh -j host:ip # creds for SQL host port from initial node start 
```
